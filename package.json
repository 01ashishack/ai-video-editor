{
  "name": "ai_videoeditor",
  "version": "0.0.0",
  "private": true,
  "description": "Phase 0 foundation for AI_VIDEOEDITOR.",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p packages/core/tsconfig.json --noEmit --composite false --incremental false && tsc -p packages/remotion-renderer/tsconfig.json --noEmit --composite false --incremental false --rootDir . && tsc -p packages/ai-command-interpreter/tsconfig.json --noEmit --composite false --incremental false && tsc -p packages/aide-cli/tsconfig.json --noEmit --composite false --incremental false && tsc -p packages/aide-desktop/tsconfig.json --noEmit --composite false --incremental false",
    "build": "tsc -b"
  },
  "dependencies": {
    "ajv": "^8.17.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
